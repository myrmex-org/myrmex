{
  "summary": "Returns a sample JSON object",
  "description": "This endpoint calls a sample lambda function and returns it's result",
  "responses": {
    "200": {
      "description": "Lambda execution succeed",
      "schema": {
        "$ref": "#/definitions/Sample"
      }
    }
  }
}
