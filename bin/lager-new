#!/usr/bin/env node
'use strict';

const program = require('commander');
const wrench = require('wrench');

wrench.copyDirSyncRecursive(
  __dirname + '/template-new',
  process.cwd(),
  { forceDelete: true }
);
